/**
 * @enum {string}
 */
export const Position = Object.freeze({
  bottom: 'bottom',
  top: 'top',
});

/**
 * @enum {string}
 */
export const Orientation = Object.freeze({
  vertical: 'vertical',
  horizontal: 'horizontal',
});
